version: '3.8'

services:
  emart_store:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: emart_store
    restart: unless-stopped
    tty: true
    working_dir: /var/www
    environment:
      SERVICE_NAME: emart_store
      SERVICE_TAGS: dev
      DB_CONNECTION: ${DB_CONNECTION}
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT}
      DB_DATABASE: ${DB_DATABASE}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
    volumes:
      - .:/var/www
      - ./php/local.ini:/usr/local/etc/php/conf.d/local.ini
    networks:
      - emart_store
    depends_on:
      - mariadb

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    container_name: emart_store_nginx
    restart: unless-stopped
    tty: true
    ports:
      - "8093:80"
    volumes:
      - .:/var/www
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - emart_store
    depends_on:
      - emart_store

  mariadb:
    image: mariadb:latest
    container_name: emart_store_db
    restart: unless-stopped
    tty: true
    environment:
      MARIADB_ROOT_PASSWORD: ${DB_PASSWORD} 
      MARIADB_DATABASE: ${DB_DATABASE}
      MARIADB_USER: ${DB_USERNAME}
      MARIADB_PASSWORD: ${DB_PASSWORD}
    volumes:
      - mariadb:/var/lib/mysql
    networks:
      - emart_store

networks:
  emart_store:
    driver: bridge

volumes:
  mariadb:
    driver: local
